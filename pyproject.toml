[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "vessel-utils"
version = "1.0.0"
description = "Blood vessel segmentation for mouse brain microscopy: the published pipeline, frozen, plus active development"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "itk",
    "matplotlib",
    "numpy",
    "opencv-python",
    "scikit-image",
    "scipy",
    "SimpleITK",
    "tqdm",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools]
packages = ["velazquez_rivera_2025", "vessel_utils"]

[tool.pytest.ini_options]
testpaths = ["tests"]
